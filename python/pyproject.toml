[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["mypy>=1.20.2", "pytest>=9.0.3", "ruff>=0.15.12"]

[tool.pytest.ini_options]
pythonpath = ["src"]
